package Models;

// bir sınıfı kalıtım yolu ile aktarılamayan yetenekler,davranışlar
// interface vasıtası ile verilebilir.
public interface Walkable {
    void walk();
}

// Java uygulamalarında diğer bir abstraction sağladığımız özellik ise Interface kullanımıdır.
// Interface ve Abstract sınıflar soyutlama yapıları olduğu kendi başlarına bir anlam ifade etmezler.
